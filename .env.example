# Configurações de ambiente para a API Biblioteca IoT
# Copie este arquivo para .env e preencha com seus valores

# IP da máquina para acesso via rede local
# Para descobrir o IP: ifconfig (macOS/Linux) ou ipconfig (Windows)
LOCAL_IP=SEU_IP_AQUI

# Porta da aplicação
PORT=8000

# URL base para desenvolvimento
API_BASE_URL=http://localhost:8000

# URL base para rede local (será construída usando LOCAL_IP)
# Exemplo: http://192.168.1.100:8000
